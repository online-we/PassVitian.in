[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"
  [build.environment]
  # Safelist cloud name + preset so secrets scanner allows them in build (they are public by design).
  SECRETS_SCAN_SMART_DETECTION_OMIT_VALUES = "PassVitian,dxvqey8bl"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
